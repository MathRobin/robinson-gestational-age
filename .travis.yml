language: node_js
node_js:
  - "14"

cache:
  yarn: true
  directories:
    - node_modules

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - yarn install --frozen-lockfile
